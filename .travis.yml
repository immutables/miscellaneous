language: java
install: true
script: if [[ "$TRAVIS_REPO_SLUG" == "immutables/miscellaneous" && "$TRAVIS_BRANCH" == "master" ]]; then mvn deploy -DdeployAtEnd=true --settings settings.xml -Pjavadoc; else mvn package; fi
jdk:
  - oraclejdk8
